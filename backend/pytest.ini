# This is the main section header for pytest configuration.
[pytest]

# This is the most important line. It tells pytest to add the project's
# root directory ('.') to the Python path before running tests. This
# ensures that your test files can successfully `import app`.
pythonpath = .

# This explicitly tells pytest where to look for tests, making discovery
# faster and more reliable.
testpaths = tests